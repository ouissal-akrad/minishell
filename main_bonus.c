#include "minishell_bonus.h"

int main(int argc, char *argv[], char *env[])
{
	(void)argc;
	(void)argv;
	(void)env;
	// char *line;
	// t_data data;

	// while ((line = readline("minishell$ ")))
	// {

	// 	free(line);
	// }
	return (0);
}
